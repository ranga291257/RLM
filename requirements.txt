# Requirements for Windows with CUDA 13.0
# Python 3.13 support with UV package manager
# IMPORTANT: CUDA 13.0 requires NVIDIA driver version 600+ (check with: nvidia-smi)
# 
# Installation instructions:
# 1. Run the setup script (installs UV if needed, creates venv_uv, installs PyTorch with CUDA):
#    .\setup_cuda130_uv.ps1
# 2. Activate the virtual environment:
#    .\venv_uv\Scripts\Activate.ps1
# 3. Verify CUDA installation:
#    python check_cuda.py
#
# Note: PyTorch (torch, torchvision, torchaudio) is installed separately via the setup script
# with CUDA 13.0 support. This file contains only the remaining dependencies.
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pdfkit>=1.0.0
PyPDF2>=3.0.0
python-dateutil>=2.9.0
python-dotenv>=1.0.1
requests>=2.31.0
ollama>=0.6.0

# Optional: OCR support for scanned PDFs (no OpenCV required)
# Install Tesseract OCR first: https://github.com/UB-Mannheim/tesseract/wiki
# Then install these Python packages:
# pytesseract>=0.3.10
# pdf2image>=1.16.3
# Pillow>=10.0.0
